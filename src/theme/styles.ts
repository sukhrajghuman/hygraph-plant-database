import { Styles } from "@chakra-ui/theme-tools";

const styles: Styles = {
  global: {
    "html,body,#__next": {
      minHeight: "100vh",
      height: "100%",
    },
  },
};

export default styles;
